name: Slynx Auto Post to X

on:
  workflow_dispatch:
  schedule:
    - cron: "0 23,11 * * *" # 10:00 and 22:00 Melbourne-ish depending on DST

jobs:
  autopost:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Debug files
        run: |
          pwd
          ls -la
          ls -la scripts

      - name: Install deps
        run: npm install

      - name: Debug env presence (safe)
        run: node -e "console.log('GOOGLE_CLIENT_ID present:', !!process.env.GOOGLE_CLIENT_ID)"

      - name: Run autoposter
        run: node scripts/post_to_x.js
        env:
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          GOOGLE_REFRESH_TOKEN: ${{ secrets.GOOGLE_REFRESH_TOKEN }}
          GOOGLE_SHEET_ID: ${{ secrets.GOOGLE_SHEET_ID }}
          GOOGLE_SHEET_TAB: ${{ secrets.GOOGLE_SHEET_TAB }}
          X_CLIENT_ID: ${{ secrets.X_CLIENT_ID }}
          X_CLIENT_SECRET: ${{ secrets.X_CLIENT_SECRET }}
          X_REFRESH_TOKEN: ${{ secrets.X_REFRESH_TOKEN }}

